// import { createSelector } from "@ngrx/store";
// import { GlobalAdminState } from "./global-admin.state";
// import { AppState } from "./global-admin.reducer";


// export const selectGlobalAdminFeature = (state: AppState) => state.globalAdmin;

// export const selectRolesGlobalAdmin = createSelector(
//     selectGlobalAdminFeature,
//     (state: GlobalAdminState) => state.roles
// );
